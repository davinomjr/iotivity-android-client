<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="AppButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:colorButtonNormal">@color/bootstrap_brand_primary</item>
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">#fff</item>
    </style>

    <style name="AppButton.SuccessButton">
        <item name="android:colorButtonNormal">@color/bootstrap_brand_success</item>
    </style>

    <style name="AppButton.InfoButton">
        <item name="android:colorButtonNormal">@color/bootstrap_brand_info</item>
    </style>


    <style name="AppButton.DangerButton">
        <item name="android:colorButtonNormal">@color/bootstrap_brand_danger</item>
    </style>

</resources>
